'use strict';

var SongFactory = require('./SongFactory');
var StationFactory = require('./StationFactory');
var PlaylistFactory = require('./PlaylistFactory');

// PlaylistFactory.getPlaylists(new Date(), new Date(), function(error, playlists) {
//     console.log(error, playlists);
// });

// StationFactory.getStations(function(error, stations) {
//     console.log(error, stations);
// });

// SongFactory.getSongs('studiobrussel', new Date(), function(error, songs) {
//     console.log(error, songs);
// });
